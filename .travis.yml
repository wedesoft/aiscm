sudo: required
dist: trusty
branches:
  only:
    - ubuntu/trusty
language: c
compiler:
  - gcc
before_install:
  - sudo sh doc/dependencies.sh
install:
  - ./bootstrap
  - ./configure
  - make -j
script:
  - make check -j
