dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install libmagickcore5 libfftw3-double3 libmagickcore5-extra libpango-1.0-0 libpangocairo-1.0-0 ifupdown upstart-job upstart
    - sudo apt-get install autoconf automake libtool colorgcc pandoc libswscale-dev xorg-dev libgl1-mesa-dev libglu1-mesa-dev libmagickcore-dev libmjpegtools-dev guile-2.0-dev libreadline-dev

test:
  override:
    - make -f Makefile.dist
    - ./configure
    - make check -j
