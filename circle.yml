dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install aptitude libept1.4.12
    - sudo aptitude install -y autoconf automake libtool colorgcc pandoc libswscale-dev xorg-dev libgl1-mesa-dev libglu1-mesa-dev libmagickcore-dev libmjpegtools-dev guile-2.0-dev libreadline-dev

test:
  override:
    - make -f Makefile.dist
    - ./configure
    - make check -j
