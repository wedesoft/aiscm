(use-modules (system foreign)
             (oop goops)
             (srfi srfi-26)
             (aiscm obj)
             (aiscm element)
             (aiscm int)
             (aiscm bool)
             (aiscm sequence)
             (aiscm jit)
             (aiscm asm)
             (guile-tap))
(define obj (make <obj> #:value 'sym))
(define address (pointer-address (scm->pointer 'sym)))
(define ctx (make <context>))

(ok (eqv? 8 (size-of <obj>))
    "size of SCM reference is 64 bits")
(ok (equal? #vu8(#xaa #x00 #x00 #x00 #x00 #x00 #x00 #x00) (pack (make <obj> #:value 42)))
    "pack Scheme object")
(ok (equal? (make <obj> #:value 42) (unpack <obj> #vu8(#xaa #x00 #x00 #x00 #x00 #x00 #x00 #x00)))
    "unpack Scheme object")
(ok (eq? <obj> (coerce <obj> <obj>))
    "objects coerce to objects")
(ok (eq? <obj> (coerce <obj> <int>))
    "object and integer coerce to object")
(ok (eq? <obj> (coerce <int> <obj>))
    "integer and object coerce to object")
(ok (equal? "#<<obj> abc>"
            (call-with-output-string (lambda (port) (write (make <obj> #:value 'abc) port))))
    "write wrapped object")
(ok (eq? <obj> (native-type 'a))
    "native type for a symbol is <obj>")
(ok (equal? (make <obj> #:value 'sym) (build <obj> address))
    "build SCM value")
(ok (equal? (list address) (content <obj> 'sym))
    "content of symbol returns internal 64 bit representation")
(ok (eq? -300 ((jit ctx (list <obj>) (cut call <obj> obj-negate <>)) 300))
    "compile and run call to scm_difference with one argument")
(ok (eq? -124 ((jit ctx (list <obj>) (cut call <obj> scm-lognot <>)) 123))
    "compile and run bitwise not")
(ok (equal? '(#f #t) (map (jit ctx (list <obj>) (cut call <bool> obj-zero-p <>)) '(3 0)))
    "compile and run comparison with zero")
(ok (equal? '(#t #f) (map (jit ctx (list <obj>) (cut call <bool> obj-nonzero-p <>)) '(3 0)))
    "compile and run not-equal-to zero")
(ok (equal? '(#t #f #f #f #f) (map (jit ctx (list <obj>) (cut call <bool> obj-not <>)) '(#f #t () 0 1)))
    "compile logical not for Scheme objects")
(ok (eq? 300 ((jit ctx (list <obj> <obj>) (cut call <obj> scm-sum <...>)) 100 200))
    "compile and run call to scm_sum")
(ok (eq? 100 ((jit ctx (list <obj> <obj>) (cut call <obj> scm-difference <...>)) 300 200))
    "compile and run call to scm_difference")
(ok (eq? 600 ((jit ctx (list <obj> <obj>) (cut call <obj> scm-product <...>)) 20 30))
    "compile and run call to scm_product")
(ok (eq? 30 ((jit ctx (list <obj> <obj>) (cut call <obj> scm-divide <...>)) 600 20))
    "compile and run call to scm_divide")
(ok (eq? 33 ((jit ctx (list <obj> <obj>) (cut call <obj> scm-remainder <...>)) 123 45))
    "compile and run call to scm_remainder")
(ok (eq? 72 ((jit ctx (list <obj> <obj>) (cut call <obj> scm-logand <...>)) 123 456))
    "compile and run call to scm_logand")
(ok (eq? 507 ((jit ctx (list <obj> <obj>) (cut call <obj> scm-logior <...>)) 123 456))
    "compile and run call to scm_logior")
(ok (eq? 435 ((jit ctx (list <obj> <obj>) (cut call <obj> scm-logxor <...>)) 123 456))
    "compile and run call to scm_logxor")
(ok (equal? '(#f b) (map (jit ctx (list <obj> <obj>) (cut call <obj> obj-and <...>)) '(#f a) '(b b)))
    "compile logical and for Scheme objects")
(ok (equal? '(b a) (map (jit ctx (list <obj> <obj>) (cut call <obj> obj-or <...>)) '(#f a) '(b b)))
    "compile logical or for Scheme objects")
(ok (eq? 123 ((jit ctx (list <obj> <obj>) (cut call <obj> scm-min <...>)) 123 456))
    "compile and run call to scm_min")
(ok (eq? 456 ((jit ctx (list <obj> <obj>) (cut call <obj> scm-max <...>)) 123 456))
    "compile and run call to scm_max")
(ok (eq? 1968 ((jit ctx (list <obj> <obj>) (cut call <obj> scm-ash <...>)) 123 4))
    "compile and run call to scm_ash")
(ok (eq? 123 ((jit ctx (list <obj> <obj>) (cut call <obj> obj-shr <...>)) 1968 4))
    "compile and run call to obj_shr")
(ok (equal? '(#f #t) (map (jit ctx (list <obj> <obj>) (cut call <bool> obj-equal-p <...>)) '(21 42) '(42 42)))
    "compile and run equality of objects")
(ok (equal? '(#t #f) (map (jit ctx (list <obj> <obj>) (cut call <bool> obj-nequal-p <...>)) '(21 42) '(42 42)))
    "compile and run inequality of objects")
(ok (equal? '(#t #f #f) (map (jit ctx (list <obj> <obj>) (cut call <bool> obj-less-p <...>)) '(3 5 7) '(5 5 5)))
    "compile and run lower-than comparison for objects")
(ok (equal? '(#t #t #f) (map (jit ctx (list <obj> <obj>) (cut call <bool> obj-leq-p <...>)) '(3 5 7) '(5 5 5)))
    "compile and run lower-equal comparison for objects")
(ok (equal? '(#f #f #t) (map (jit ctx (list <obj> <obj>) (cut call <bool> obj-gr-p <...>)) '(3 5 7) '(5 5 5)))
    "compile and run greater-than comparison for objects")
(ok (equal? '(#f #t #t) (map (jit ctx (list <obj> <obj>) (cut call <bool> obj-geq-p <...>)) '(3 5 7) '(5 5 5)))
    "compile and run greater-equal comparison for objects")
(ok (not (pointerless? <obj>))
    "object memory is not pointerless")
(ok (not (signed? <obj>))
    "object references don't need sign-extension")
(ok (eq? 123 ((jit ctx (list <obj>) (cut call <long> scm-to-int64 <>)) 123))
    "compile and run call to scm_to_int64")
(run-tests)
