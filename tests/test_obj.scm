(use-modules (aiscm obj)
             (aiscm element)
             (oop goops)
             (system foreign)
             (guile-tap))
(define obj (make <obj> #:value 'sym))
(ok (equal? (list (pointer-address (scm->pointer 'sym))) (content 'sym))
    "content of symbol returns internal 64 bit representation")
(run-tests)
