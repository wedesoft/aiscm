(use-modules (oop goops)
             (rnrs bytevectors)
             (system foreign)
             (aiscm mem)
             (aiscm element)
             (aiscm sequence)
             (aiscm image)
             (guile-tap))
(define l '((2 3 5 7 11 13 17 19)))
(define m (list->multiarray l))
(define mem (get-value m))
(define img (make <image> #:format 'GRAY #:shape '(8 1) #:mem mem))
(planned-tests 12)
(ok (equal? #vu8(2 2 2 3 3 3) (read-bytes (get-mem (convert img 'BGR)) 6))
  "conversion to BGRA")
(ok (equal? '(16 2) (shape (convert img 'BGRA '(16 2))))
  "shape of scaled image")
(ok (eqv? (get-mem img) (get-mem (convert img 'GRAY)))
  "do nothing if converting to identical format")
(ok (equal? #vu8(2 3 5 7 11 13 17 19 2 3 5 7 11 13 17 19)
            (read-bytes (get-mem (convert img 'GRAY '(8 2))) 16))
  "values of image with scaled height")
(ok (equal? 2 (bytevector-u8-ref (read-bytes (get-mem (convert img 'GRAY '(8 2) '(0) '(16))) 32) 16))
  "correct application of custom pitches")
(ok (equal? '((2 3 5 7 11 13 17 19)) (multiarray->list (image->multiarray img)))
  "'image->multiarray' should convert the image to a 2D array")
(skip (equal? '((2 3 5 7 11 13 17 19)) (multiarray->list (image->multiarray (convert img 'UYVY))))
  "'image->multiarray' should convert the image to grayscale")
(ok (equal? '(2 2) (multiarray->list (project (roll (image->multiarray (convert img 'GRAY '(8  2) '(0) '(16)))))))
  "'image->multiarray' should take pitches (strides) into account")
(ok (equal? 'GRAY (get-format (multiarray->image m)))
  "'multiarray->image' converts to grayscale image")
(ok (equal? '(8 1) (shape (multiarray->image m)))
  "'multiarray->image' preserves shape of array")
(ok (equal? l (multiarray->list (image->multiarray (multiarray->image m))))
  "Converting from UBYTE multiarray to image and back preserves data")
(ok (equal? #vu8(1 3 2 4) (read-bytes (get-mem (multiarray->image (roll (list->multiarray '((1 2) (3 4)))))) 4))
  "Conversion to image ensures compacting of pixel lines")
