(use-modules (oop goops) (aiscm magick) (aiscm xorg) (aiscm core))
(define image (read-image "shuttle.jpg"))
(define width (car (shape image)))
(define height (cadr (shape image)))
(define left (crop (list (/ width 2) height) image))
(define right (dump (list (/ width 2) 0) image))
(show (rgb (red left) (/ (green right) 2) (/ (blue right) 2)))
